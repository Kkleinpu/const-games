# 工作规则

## 核心原则
- 先问清楚再写代码，不确定就问
- 一次只做一件事，不要同时改多个东西
- 每次修改都要明确说"改了什么、为什么改"
- 不确定需求时，用最少3个问题澄清，不要猜

## 代码风格
- 代码注释用中文
- 不要写多余的功能，只要我要求的
- 不要重复已经存在的代码
- 每个文件改动说明原因

## 输出规范
- 不要输出大段解释，直接给代码
- 改动前先说改什么，改完说改了什么
- 一次不要超过3个文件的改动
- 优先用现有代码，不要重写

## 重要
- 永远要求反馈循环：静态类型检查、浏览器验证
- 先小步改动，确认可用后再继续下一步
- 遵循红-绿-重构循环（先写失败的测试，再让它通过）
# Gaming Hub 工作规则

## 项目架构 - AI 必须先读
- 项目是纯 HTML+CSS+JS 的 Steam 游戏主页
- index.html - 页面结构（不要动布局，只改内容）
- style.css - 样式文件（所有样式改动在这里）
- script.js - 业务逻辑（游戏数据、工具功能、渲染逻辑）
- server.js - Express 后端，Steam API 代理

## 修改规范
- 改样式只动 style.css，不要动 index.html 里的行内样式
- 改 JS 功能只动 script.js，不要改 index.html 里 onclick 引用
- 改前先告诉我要改哪里，确认后只出手一次
- 不要重写整个文件，只替换要改的那几行
- 不要动 server.js，后端功能已经正常
- 不要引入新的 npm 包/框架

## 禁止行为
- 禁止说"现在让我重新整理整个项目"
- 禁止把索引页面重构成 SPA
- 禁止添加 React/Vue/任何框架
- 禁止改游戏数据数组，数据是假数据

## 输出规范
- 每次改动告诉我：改哪个文件、改哪几行、为什么改
- 改完给一段说明，让我去浏览器验证